namespace = alb_parliament

country_event = {
    id = alb_parliament.1
    hidden = yes
    fire_only_once = yes
    
    trigger = {
        current_age = age_of_revolutions
        has_country_flag = alb_parliament_expanded
        is_great_power = yes
        has_disaster = no
    }
    
    immediate = {
        add_disaster_progress = {
            disaster = revolution
            value = 10
        }
        clr_country_flag = alb_parliament_expanded
    }
    
    option = {
        name = "OK"
    }
}